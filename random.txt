// variáveis importantes
const itemInventario = document.querySelector('.itemInventario');
const nomeItem = document.querySelector('.nomeItem');
const nextBtn = document.querySelector('.next');
const dialog1 = document.getElementById('dialog1'); //  diálogos
const dialog2 = document.getElementById('dialog2');
const dialog3 = document.getElementById('dialog3');
const dialog4 = document.getElementById('dialog4');
// EVENTOS PARA A FOGUEIRA
// animações da mão
function pointerHand() {
    let hand = document.querySelector('.hand');
    hand.style.backgroundImage = "url('issets/handPointerFire.png')";
}
function normalHand() {
    let hand = document.querySelector('.hand');
    hand.style.backgroundImage = "url('issets/handIconFire.png')";
}
// trocar diálogo
const nextDialog = () => {
    console.log(document.getElementById('dialog1').style.getPropertyValue('color'));
}
// eventos inventário => talvez tenha q fazer um arquivo de eventos de inventário para cada nível, por causa do tanto de if

const mostrarNome = () => {
    nomeItem.style.color = "white";
} //  fazer isso funcionar => Só era limpar o código KKKKKKKKKKKKKKKKKKKKKK
const esconderNome = () => {
    nomeItem.style.color = "transparent";
}
//  ADICIONANDO EVENT-LISTENERS
itemInventario.addEventListener('onmouseover', pointerHand());  //  item hand
itemInventario.addEventListener('onmouseout', normalHand());
itemInventario.addEventListener('onmouseover', mostrarNome());  //  item nome
itemInventario.addEventListener('onmouseout', esconderNome());
nextBtn.addEventListener('onmouseover', pointerHand()); //  next
nextBtn.addEventListener('onmouseout', normalHand());
nextBtn.addEventListener('click', nextDialog());